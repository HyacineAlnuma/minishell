# Make recompile pas sur le touch btmalloc.h, 
# echo -nnnnnnnn devrait foncitonner, attention echo -na.
# exit, message d'erreur  si overflow de l'argument numerique.
# exit doit quitter si  premier argument est une valeur non numeric, mais quitte si deux argument dont le premier est pas numerique.
# minishell dans minishell ctrl-c erreur. 
# unset USER, invalid read
# $"USER" donne USER
# "export test" mets test dans l'env., les variable d' export commence par une lettre, et peuvent ensuite contenir chiffre ou _
# env -i invalid read (mais surement meme probleme que unset USER)
# si redirection sur fichier sans les droits, mauvais comportement., si dernier fd chmod 000, mauvais comportement.
# Check pipe nb
# Check delimiter eof
# un open de trop sur << eof << eof <<< eof
# Premièment vous utiliser la variable d'environnement PATH pour verifier l'existence d'un binaire mais pas pour l'executer, vous executer vos binaires uniquement depuis /usr/bin, c'est dommage mais facile a regler.
# De plus ce comportement vous cause probleme avec exit, qui ne fonctionne plus du premier coup si on essaie d'executer un binaire qui existe mais qui n'est pas dans /usr/bin
# (en securite faites un clean exit apres execve, mais si vos path sont bien check il ne devrait pas y avoir de problemes)

# Votre minishell fonctionne mal si on l'execute avec env -i
# les variables d'env dans les here docs sont a gerer !
# Il faut egalement ignorer les brokens pipes quand le heredoc avec un crtl C
# Et pour finir n'oubliez pas de transmettre les signaux aux enfant (Crtl \) : Vous ignorez le Ctrl \ si vous etes dans l'execution d'un binaire alors qu'il faudrait le tranmettre au binaire.

# cd tout seul
# lorsque env -i ./minishell, ne pas check les paths qui contiennent USER
protéger les mallocs
# export;
	# - ne pas afficher la variable "_"
	# - pouvoir export et unset plusieurs variables d'un coup
	# - problemes de parsing avec export s="'"'"'
	# - mettre entre double quotes le content de variables
	# - implementer +=
# echo $'
# heredoc dans heredoc
# leaks lorsque cat tout dans heredoc
# print exit dans exit program
utiliser print error

env -i cd recreer oldpwd
cd 		$HOME
si $HOME > erreur not HOME set
exit code in unset and export

# valgrind --suppressions=ignore_readline.supp --trace-children=yes --show-leak-kinds=all --leak-check=full --track-fds=yes ./minishell